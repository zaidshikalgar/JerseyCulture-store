import { CartContent } from '@/components/cart/cart-content';
export const metadata = {
    title: 'Shopping Cart | JerseyCulture',
};
export default function CartPage() {
    return <CartContent />;
}
