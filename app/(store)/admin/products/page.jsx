import { ProductsManager } from '@/components/admin/products-manager';

export const metadata = {
  title: 'Admin Products | JerseyCulture',
};

export default function AdminProductsPage() {
  return <ProductsManager />;
}
