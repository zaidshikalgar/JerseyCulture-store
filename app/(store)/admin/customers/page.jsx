import { CustomersManager } from '@/components/admin/customers-manager';

export const metadata = {
  title: 'Admin Customers | JerseyCulture',
};

export default function AdminCustomersPage() {
  return <CustomersManager />;
}
